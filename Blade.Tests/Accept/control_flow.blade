cog var x: u32 = 0;
if (x == 0) { x = 1; }
if (x > 0) { x = 1; } else { x = 2; }
if (x == 0) { x = 1; } else if (x == 1) { x = 2; } else { x = 3; }
while (x != 0) { x = x - 1; }
for (x) { }
loop { break; }
rep loop { x = x + 1; }
noirq { x = 1; }
