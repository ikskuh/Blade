// EXPECT: pass
// DIAGNOSTICS: ComptimeIntegerTruncation
// NOTE:
//   Explicit casts also warn when a compile-time-known value is truncated.
cog task main {
    
    cog const SMALL: u8 = 257 as u8;
    
    assert SMALL == 1;
}