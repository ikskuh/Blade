// EXPECT: pass
// DIAGNOSTICS:
// - LexicalNameConflictsWithLayoutMember

layout Container
{
    lut var global: i8 = 0;
}

hub var global: u32 = 0;

fn shadow()
    : layout(Container)
{
    var local_1: u32 = global; // must resolve to 
    var local_2: i8 = Container.global; // must resolve to the container variable
}

cog task main { }
