// EXPECT: fail
// DIAGNOSTICS:
// - AccessToForeignLayout

layout Container
{
    lut var global: i8 = 0;
}

layout Other
{
    lut var magic: i8 = 0;
}

fn func_wrong_layout()
    : layout(Other)
{
    Container.global = 1; // must be rejected as the task does not inherit the layout
}

cog task main { }
