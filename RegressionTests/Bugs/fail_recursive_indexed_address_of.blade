// EXPECT: fail
// DIAGNOSTICS:
// - L9, AddressOfRecursiveLocal
// NOTE:
//   Indexed array elements remain subject to the recursive-local address-of
//   restriction when the base array is a recursive function parameter.
cog task main {
    rec fn demo(values: [4]u32) void {
        var ptr: *cog u32 = &values[1];
    }
}