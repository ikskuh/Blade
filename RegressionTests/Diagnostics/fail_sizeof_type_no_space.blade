// EXPECT: fail
// DIAGNOSTICS:
// - QueryRequiresMemorySpace
// NOTE:
//   sizeof(Type) without a memory space is invalid — needs sizeof(Type, .space).


cog task main {
    cog var x: u32 = sizeof(u32);
}

