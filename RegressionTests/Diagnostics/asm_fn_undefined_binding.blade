// EXPECT: fail
// DIAGNOSTICS: InlineAsmUndefinedVariable
// NOTE:
//   asm fn that references an undefined variable binding should report InlineAsmUndefinedVariable.
asm fn bad_ref(a: u32) -> u32 {
    MOV {return}, {nonexistent}
}


cog task main {
    cog var sink: u32 = bad_ref(1);
}
