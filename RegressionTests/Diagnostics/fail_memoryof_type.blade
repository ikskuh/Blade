// EXPECT: fail
// DIAGNOSTICS:
// - MemoryofRequiresVariable
// NOTE:
//   memoryof(Type, memoryspace) is invalid — memoryof only accepts a variable.

import builtin;


cog task main {
    cog var x: u32 = 0;
    cog var cls: builtin.MemorySpace = memoryof(u32, .hub);
}

