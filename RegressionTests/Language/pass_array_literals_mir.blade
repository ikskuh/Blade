// EXPECT: pass
// STAGE: mir
// CONTAINS:
// - sidefx store index.cog(%?, %?, %?)
// - sidefx store.place g_explicit_values(%?)
// - sidefx store.place g_spread_values(%?)
// - sidefx store.place g_empty_values(%?)
// NOTE:
//   Array literals lower to indexed stores for explicit elements, spread fill,
//   and empty contextual fill.
cog task main {
    
    cog var explicit_values: [3]u32 = [1, 2, 3];
    cog var spread_values: [4]u32 = [1, 2...];
    cog var empty_values: [2]u32 = [];
}
