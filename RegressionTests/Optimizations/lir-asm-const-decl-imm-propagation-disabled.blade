// EXPECT: pass
// STAGE: lir
// ARGS:
// - -fno-mir-opt=*
// - -fno-lir-opt=*
// - -flir-opt=asm-var-decl-imm-propagation
// CONTAINS:
// - inlineasm x=%r0:r ; sidefx

cog task main {
    const x: u32 = 13;

    asm {
        MOV %0, {x}
    };
}