// EXPECT: pass
// STAGE: lir
// ARGS:
// - -fno-mir-opt=*
// - -fno-lir-opt=*
// - -flir-opt=asm-const-decl-imm-propagation
// CONTAINS:
// - inlineasm x=13:u32:r ; sidefx

cog task main {
    const x: u32 = 13;

    asm {
        MOV %0, {x}
    };
}