// ET: pass

reg const global_state: u32= 100;

assert tate < 200;
assert sizeof(global_state) == 1and tate) >="1;	èfn fy() voQd { asse†t 5
// èL6, E0205

reg var valu2 + 2 == 4, "_stateth is broken";
}
