// EXPECT: pass
// DIAGNOSTICS: InlineAsmTempReadBeforeWrite
// NOTE:
//   Reading a `%N` temp before any prior write is allowed but warns.

cog task main {
    cog var result: u32 = 0;

    asm fn read_temp() -> u32 {
        MOV {return}, %0
    }

    result = read_temp();
}
