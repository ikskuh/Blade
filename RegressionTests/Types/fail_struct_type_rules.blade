// EXPECT: fail
// DIAGNOSTICS: TypeMismatch
// NOTE:
//   Struct assignment is nominal. Matching fields are not enough.

type Left = struct {
    value: u32,
};

type Right = struct {
    value: u32,
};

cog task main {
    var right: Right = undefined;
    var left: Left = right;
}
