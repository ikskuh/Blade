// EXPECT: pass
// STAGE: final-asm
// CONTAINS:
// - ? LONG 104, 101, 108, 108, 111
// NOTE:
//   String literals now bind directly to literal-backed symbolic pointers.

cog task main {
    var s: [*]cog const u8 = "hello";
}
