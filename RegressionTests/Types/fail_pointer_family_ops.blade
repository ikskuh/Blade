// EXPECT: fail
// DIAGNOSTICS:
// - L9, TypeMismatch
// - L10, TypeMismatch
// NOTE:
//   `*T` supports deref only, `[*]T` supports indexing only.

fn demo(ptr: *cog u32, many: [*]cog u32) -> u32 {
    var a: u32 = ptr[0];
    var b: u32 = many.*;
    return a + b;
}

cog task main {} 
