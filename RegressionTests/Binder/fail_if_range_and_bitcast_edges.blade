// EXPECT: fail
// DIAGNOSTICS: TypeMismatch, RangeExpressionOutsideForLoop
// NOTE:
//   Covers the remaining if-expression and standalone range-expression type checks.
cog task main {
    
    fn demo(flag: bool, ptr: *cog u32) {
        var chosen: *cog u32 = if (flag) undefined else ptr;
        var mismatch: u32 = if (flag) 1 else false;
        _ = false..<true;
    }
}