// EXPECT: pass
// STAGE: bound
// NOTE:
//   Covers array-parameter address-of and multi-pointer indexing paths.
cog task main {
    
    fn demo(values: [2]u32, many: [*]cog u32) {
        var first: u32 = many[0];
        many[0] = first;
    
        var alias: [*]cog u32 = &values;
        alias[0] += 1;
    }
}