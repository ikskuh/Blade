// EXPECT: fail
// DIAGNOSTICS: CannotAssignToConstant
// NOTE:
//   Writing to an extern const variable must be rejected.
cog task main {
    
    extern cog const INA: u32 @(0x1FE);
    
    fn write_to_const() void {
        INA = 1;
    }
}