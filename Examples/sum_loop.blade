// Starter example: top-level automatic values plus a simple while loop.

fn sum_to(limit: u32) -> u32 {
    var total: u32 = 0;
    var i: u32 = 0;

    while (i < limit) {
        total += i;
        i += 1;
    }

    return total;
}

var requested_limit: u32 = 8;
reg var total: u32 = 0;

total = sum_to(requested_limit);
