{
  "permissions": {
    "allow": [
      "Bash(dotnet build)",
      "Bash(dotnet test)",
      "Bash(dotnet build --nologo -v q)",
      "Bash(dotnet test --nologo -v q)",
      "Bash(dotnet test --nologo -v n)"
    ]
  }
}
