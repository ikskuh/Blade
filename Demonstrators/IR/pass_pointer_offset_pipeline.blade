// EXPECT: pass
// STAGE: mir
// CONTAINS:
// - ptr.offset
// NOTE:
//   Covers pointer offset operations through the MIR pipeline.
//   Exercises MirPointerOffsetInstruction lowering.

fn pointer_offset_write(p: [*]reg u32, offset: u32) -> u32 {
    var q: [*]reg u32 = p + offset;
    return q[0];
}
