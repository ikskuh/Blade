// EXPECT: pass
// STAGE: mir
// ARGS:
// - --no-single-callsite-inline
// - -fno-mir-opt=*
// - -fmir-opt=dce
// NOT_CONTAINS:
// - binary.Add
// NOTE:
//   Positive case for MIR dce: the dead addition should be removed.

reg var sink: u32 = 0;
reg var input: u32 = 5;

fn demo(x: u32) -> u32 {
    var y: u32 = x + 1;
    return x;
}

sink = demo(input);
