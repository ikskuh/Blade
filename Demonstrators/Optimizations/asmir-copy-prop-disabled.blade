// EXPECT: pass
// STAGE: asmir
// ARGS:
// - -fno-asmir-opt=*
// CONTAINS:
// - MOV _r1, _r2
// NOTE:
//   Negative case for ASMIR copy-prop: the temporary register copy chain remains.

reg var sink: u32 = 0;
var src: u32 = 1;
var tmp: u32 = 0;

asm {
  MOV {tmp}, {src}
};

sink = tmp;
