// EXPECT: pass
// STAGE: lir
// ARGS:
// - -fno-mir-opt=*
// - -fno-lir-opt=*
// CONTAINS:
// - branch %r0 ? bb2() : bb3()
// NOTE:
//   Baseline for LIR copy-prop. Current source lowering does not expose a stable LIR mov chain here.

reg var sink: u32 = 0;
reg var input: bool = false;
reg var left: u32 = 2;
reg var right: u32 = 3;

noinline fn demo(cond: bool, a: u32, b: u32) -> u32 {
    return if (cond) a else b;
}

sink = demo(input, left, right);
