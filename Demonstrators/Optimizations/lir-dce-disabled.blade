// EXPECT: pass
// STAGE: lir
// ARGS:
// - --no-single-callsite-inline
// - -fno-mir-opt=*
// - -fno-lir-opt=*
// CONTAINS:
// - binary.Add
// NOTE:
//   Negative case for LIR dce: the dead addition is still present.

reg var sink: u32 = 0;
reg var input: u32 = 5;

fn demo(x: u32) -> u32 {
    var y: u32 = x + 1;
    return x;
}

sink = demo(input);
