// EXPECT: pass
// STAGE: bound
// CONTAINS:
// - For
// - For -> i
// NOTE:
//   Exercises for(count) and for(count) -> index forms.

reg var count: u32 = 4;
reg var sink: u32 = 0;

for (count) { sink = sink + 1; }
for (count) -> i { sink = sink + i; }
