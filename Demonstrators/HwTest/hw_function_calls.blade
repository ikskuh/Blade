// EXPECT: pass-hw
// RUNS:
// - [] = 110
// NOTE:
//   Tests nested noinline function calls.
//   compute(10) = add(mul(10,10), 10) = add(100, 10) = 110 = 0x6E

extern reg var rt_result: u32;

noinline fn add(a: u32, b: u32) -> u32 {
    return a + b;
}

noinline fn mul(a: u32, b: u32) -> u32 {
    return a * b;
}

noinline fn compute(x: u32) -> u32 {
    return add(mul(x, x), x);
}

reg var x: u32 = 10;
rt_result = compute(x);
