// EXPECT: pass-hw
// RUNS:
// - [] = -43
// NOTE:
//   Tests unary operators on i32: negate, plus, bitwise not.
//   x=42: (-42) + (+42) + (~42) = 0 + (~42) = -43 = 0xFFFFFFD5

extern reg var rt_result: u32;

noinline fn unary(x: i32) -> i32 {
    var neg: i32 = -x;
    var plus: i32 = +x;
    var bitnot: i32 = ~x;
    return neg + plus + bitnot;
}

reg var x: i32 = 42;
rt_result = bitcast(u32, unary(x));
