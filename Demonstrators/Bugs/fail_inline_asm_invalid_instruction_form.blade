// EXPECT: fail
// DIAGNOSTICS: E0305
// NOTE:
//   Inline assembly must reject instruction forms with invalid operand counts during binding.

asm fn add_vals(a: u32, b: u32) -> u32 {
    MOV {return}, {a}, {b}
}

reg var result: u32 = 0;
result = add_vals(10, 20);
