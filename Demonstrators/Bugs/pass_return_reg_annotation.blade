// EXPECT: pass
// NOTE:
//   Parses and lowers the documented mixed register/flag return annotation shape.

fn get_three_specific() -> u32@reg, bool@C, bit@Z {
    return 100, false, 1;
}

reg var value: u32 = 0;
hub var carry: bool = false;
hub var zero: bit = 0;

value, carry, zero = get_three_specific();
