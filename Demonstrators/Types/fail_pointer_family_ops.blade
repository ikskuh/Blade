// EXPECT: fail
// DIAGNOSTICS:
// - L9, E0205
// - L10, E0205
// NOTE:
//   `*T` supports deref only, `[*]T` supports indexing only.

fn demo(ptr: *reg u32, many: [*]reg u32) -> u32 {
    var a: u32 = ptr[0];
    var b: u32 = many.*;
    return a + b;
}
