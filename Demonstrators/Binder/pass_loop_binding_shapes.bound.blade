// EXPECT: pass
// STAGE: bound
// NOTE:
//   Covers single-statement if branches plus for/rep/noirq binding shapes.

fn demo(count: u32, values: [2]u32) {
    if (count == 0)
        _ = count;
    else
        _ = values[0];

    for(values) -> &item, index {
        item = index;
    }

    for(count) -> i {
        _ = i;
    }

    for(values) {
        _ = values[0];
    }

    rep loop {
        _ = count;
        return;
    }

    rep for(1..<count) -> i {
        _ = i;
    }

    rep for(count) {
        _ = count;
    }

    noirq {
        _ = count;
    }
}
