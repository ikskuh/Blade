// EXPECT: fail
// DIAGNOSTICS: E0205, E0224, E0225
// NOTE:
//   Covers generic-width and array-size type checks plus invalid bitcast forms.

cog var width_u: uint(false) = 0;
cog var width_i: int(false) = 0;
cog var sized: [false]u32 = undefined;

cog var bad_bitcast_type: u32 = bitcast(u32, [1, 2]);
cog var bad_bitcast_size: u16 = bitcast(u16, 1 as u32);
