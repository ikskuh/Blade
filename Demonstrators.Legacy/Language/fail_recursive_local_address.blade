// EXPECT: fail
// DIAGNOSTICS:
// - AddressOfRecursiveLocal
// NOTE:
//   Recursive locals still lower into shared registers and are saved across
//   self-calls, so taking their address must remain rejected.

rec fn illegal_local_address(value: u32) {
    var local: u32 = value;
    var ptr: *cog u32 = &local;
}
