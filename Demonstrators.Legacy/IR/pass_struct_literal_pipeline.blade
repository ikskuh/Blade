// EXPECT: pass
// NOTE:
//   Struct literal lowering preserves aggregate values through IR and lowers
//   them into byte-accurate register lanes.

type Pair = struct {
    a: u32,
    b: u32,
};

cog var pair: Pair = Pair {
    .a = 1,
    .b = 2,
};

cog var sink: u32 = 0;
sink = pair.a;
