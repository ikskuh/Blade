// EXPECT: fail
// DIAGNOSTICS:
// - AddressOfParameter
// NOTE:
//   Taking a pointer to a parameter must not be allowed, as parameters are in the same space as
//   local variables.

fn test_addressof_array_param(arr: [4]u32) {
    var p: *cog u32 = &arr[0];
}
