// EXPECT: fail
// DIAGNOSTICS: StructUnknownField, StructMissingFields
// NOTE:
//   Typed struct literals reject unknown fields.

type Point = struct {
    x: u32,
    y: u32,
};

var p: Point = Point { .x = 10, .z = 20 };
